package puzzle;

public class Sequence {
    
        //first s[0]
        
        public float[] seq1 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[0]; ar[1]=s[1]; ar[2]=s[2]; ar[3]=s[3]; return ar;}
        
        public float[] seq2 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[0]; ar[1]=s[1]; ar[2]=s[3]; ar[3]=s[2]; return ar;}
    
        public float[] seq3 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[0]; ar[1]=s[2]; ar[2]=s[3]; ar[3]=s[1]; return ar;}
    
        public float[] seq4 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[0]; ar[1]=s[2]; ar[2]=s[1]; ar[3]=s[3]; return ar;}
    
        public float[] seq5 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[0]; ar[1]=s[3]; ar[2]=s[2]; ar[3]=s[1]; return ar;}
    
        public float[] seq6 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[0]; ar[1]=s[3]; ar[2]=s[1]; ar[3]=s[2]; return ar;}
    
    //first s[1]
        public float[] seq7 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[1]; ar[1]=s[2]; ar[2]=s[3]; ar[3]=s[0]; return ar;}
    
        public float[] seq8 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[1]; ar[1]=s[2]; ar[2]=s[0]; ar[3]=s[3]; return ar;}
    
        public float[] seq9 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[1]; ar[1]=s[3]; ar[2]=s[2]; ar[3]=s[0]; return ar;}
    
        public float[] seq10 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[1]; ar[1]=s[3]; ar[2]=s[0]; ar[3]=s[2]; return ar;}
    
        public float[] seq11 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[1]; ar[1]=s[0]; ar[2]=s[2]; ar[3]=s[3]; return ar;}
    
        public float[] seq12 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[1]; ar[1]=s[0]; ar[2]=s[3]; ar[3]=s[2]; return ar;}
    
    //first s[2]
        public float[] seq13 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[2]; ar[1]=s[3]; ar[2]=s[0]; ar[3]=s[1]; return ar;}
    
        public float[] seq14 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[2]; ar[1]=s[3]; ar[2]=s[1]; ar[3]=s[0]; return ar;}
    
        public float[] seq15 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[2]; ar[1]=s[1]; ar[2]=s[3]; ar[3]=s[0]; return ar;}
    
        public float[] seq16 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[2]; ar[1]=s[1]; ar[2]=s[0]; ar[3]=s[3]; return ar;}
    
        public float[] seq17 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[2]; ar[1]=s[0]; ar[2]=s[1]; ar[3]=s[3]; return ar;}
    
        public float[] seq18 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[2]; ar[1]=s[0]; ar[2]=s[3]; ar[3]=s[1]; return ar;}
    
    //first s[3]
        public float[] seq19 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[3]; ar[1]=s[0]; ar[2]=s[1]; ar[3]=s[2]; return ar;}
    
        public float[] seq20 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[3]; ar[1]=s[0]; ar[2]=s[2]; ar[3]=s[1]; return ar;}
    
        public float[] seq21 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[3]; ar[1]=s[1]; ar[2]=s[0]; ar[3]=s[2]; return ar;}
    
        public float[] seq22 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[3]; ar[1]=s[1]; ar[2]=s[2]; ar[3]=s[0]; return ar;}
    
        public float[] seq23 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[3]; ar[1]=s[2]; ar[2]=s[0]; ar[3]=s[1]; return ar;}
    
        public float[] seq24 (float[] s){ float[] ar = new float[s.length];
            ar[0]=s[3]; ar[1]=s[2]; ar[2]=s[1]; ar[3]=s[0]; return ar;}
    
}
